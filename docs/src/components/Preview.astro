---
// Wraps a live component example in a styled preview box
interface Props {
  class?: string;
}
const { class: className } = Astro.props;
---

<div class:list={["preview", className]}>
  <slot />
</div>
